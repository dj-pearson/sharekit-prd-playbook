# =============================================================================
# Supabase Configuration
# =============================================================================
# For self-hosted Supabase, use your self-hosted instance URL (e.g., https://your-domain.com)
# For hosted Supabase, use https://your-project-ref.supabase.co
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional: Project ID for Supabase CLI operations
VITE_SUPABASE_PROJECT_ID=your_project_id

# Server-side only (for scripts like sitemap generation)
# These are NOT prefixed with VITE_ as they should not be exposed to the client
# Required for running: npm run sitemap
SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_supabase_anon_key

# =============================================================================
# Stripe Configuration
# =============================================================================
# Get these from your Stripe Dashboard (https://dashboard.stripe.com)
# Use test keys (pk_test_* and sk_test_*) for development
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Stripe Price IDs
# Create these in your Stripe Dashboard under Products
# Copy the Price ID (starts with price_) for each price you create
VITE_STRIPE_PRO_MONTHLY_PRICE_ID=price_your_pro_monthly_id
VITE_STRIPE_PRO_YEARLY_PRICE_ID=price_your_pro_yearly_id
VITE_STRIPE_BUSINESS_MONTHLY_PRICE_ID=price_your_business_monthly_id
VITE_STRIPE_BUSINESS_YEARLY_PRICE_ID=price_your_business_yearly_id

# =============================================================================
# Supabase Edge Functions Environment Variables
# =============================================================================
# Note: The following variables are set in Supabase Edge Functions configuration,
# not in this .env file. Configure these in your Supabase Dashboard or via CLI:
#
# - SUPABASE_URL (automatically set in Edge Functions)
# - SUPABASE_SERVICE_ROLE_KEY (for server-side operations)
# - STRIPE_SECRET_KEY (server-side only, starts with sk_)
# - STRIPE_WEBHOOK_SECRET (for webhook signature verification, starts with whsec_)
# - APP_URL (your app's URL for redirects, e.g., https://sharekit.net)
# - STRIPE_PRO_MONTHLY_PRICE_ID
# - STRIPE_PRO_YEARLY_PRICE_ID
# - STRIPE_BUSINESS_MONTHLY_PRICE_ID
# - STRIPE_BUSINESS_YEARLY_PRICE_ID
#
# For self-hosted Supabase, set these in your deployment configuration
# or via: supabase secrets set KEY=value
